# Java 后端（Tomcat 进程）可选环境变量 — 复制为 .env 仅当你的启动脚本会加载它时才有意义；
# 常见做法是在 IDE / Tomcat 启动配置里直接设置 TA_DATA_DIR、TA_JWT_SECRET。

# 数据目录：绝对路径，可写；等价 JVM 参数 -Dta.data.dir=...
# TA_DATA_DIR=/absolute/path/to/data

# JWT 密钥：生产环境务必改为长随机串；亦可配置 web.xml 的 secretKey 上下文参数
# TA_JWT_SECRET=
# SECRET_KEY=
